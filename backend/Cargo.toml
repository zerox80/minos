[package]
name = "minos-backend"
version = "1.0.0"
edition = "2021"
rust-version = "1.88"  # Minimum Supported Rust Version
authors = ["zerox80"]
license = "MIT"
description = "High-performance Rust backend API for minos"
repository = "https://github.com/zerox80/minos"
keywords = ["cms", "api", "tutorial", "axum", "education"]
categories = ["web-programming", "web-programming::http-server"]

[profile.release]
lto = true
codegen-units = 1
strip = true

[dependencies]
axum = { version = "0.8", features = ["multipart"] }
axum-extra = { version = "0.12", features = ["typed-header", "cookie"] }
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.7", features = ["cors", "limit", "fs"] }
tower_governor = "0.8"
governor = "0.10.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio-rustls", "sqlite", "macros"] }
jsonwebtoken = { version = "=10.3.0", default-features = false }
bcrypt = "0.19"
chrono = { version = "0.4", features = ["serde"] }
dotenvy = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1.4", features = ["v4", "fast-rng", "macro-diagnostics"] }
infer = "0.19"
url = "2.5"
idna_adapter = "=1.2.2"
regex = { version = "1.10", default-features = false, features = ["std", "unicode-case", "unicode-perl"] }
anyhow = "1.0"
time = "0.3"

sha2 = "0.11"
hmac = "0.13"
subtle = "2.5"
reqwest = { version = "0.13", features = ["json"] }
html-escape = "0.2"
rand = "0.10"

# The exact pins below (and `idna_adapter` above) hold transitive
# dependencies at the last versions compatible with our MSRV (rust-version
# 1.88); newer releases require a newer toolchain or edition 2024.
# Re-evaluate them whenever the MSRV is raised.
[dependencies.home]
version = "=0.5.12"

[dependencies.base64ct]
version = "=1.8.3"
default-features = false
features = ["alloc"]

[[bin]]
name = "export_content"
path = "src/bin/export_content.rs"

[[bin]]
name = "import_content"
path = "src/bin/import_content.rs"
